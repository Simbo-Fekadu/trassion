#!/usr/bin/env node
// Deprecated script: image optimization has been removed from the project.
// This stub remains only to avoid tooling or CI references failing if still invoked.
console.log("Image optimization is disabled. Using original images only.");
process.exit(0);
